import missionNames from "./missionNames.json"
import color from "./colors.json"

// @todo color is ever translated to non-english, it will break the display.

const dictionary = { color, missionNames, messages: {} }

export default dictionary
